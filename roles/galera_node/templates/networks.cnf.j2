[mysqld]
bind-address = {{ galera_ip }}
proxy_protocol_networks = {% for galera_lb_host in groups['galera_lb'] %}{{ hostvars[galera_lb_host].galera_ip }}{% if not loop.last %},{% endif %}{% endfor %}

skip_name_resolve
